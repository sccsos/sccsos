name: 每日巡检
description: 定时巡检任务 — 环境健康检查与报告
schema_version: '1.1'
parallel_groups: []

steps:
  - id: health-check
    name: 环境健康检查
    agent: code-reviewer
    prompt: |
      执行环境健康检查，返回 JSON 报告包含：
      - system: 系统运行状态
      - hermes: Hermes Agent 连接状态
      - agents: 已注册 Agent 状态
